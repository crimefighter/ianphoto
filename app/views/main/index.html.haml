=javascript_include_tag "jquery.fullbg"
=javascript_include_tag "main_page"
-content_for :title do
  ianstudio
:javascript
  var photos_collection = #{@json_collection};

.top_bar ianstudio
-unless @photo.blank?
  =image_tag(@photo.picture(:original), :class => "title fullBg")
-unless @next_photo.blank?
  =image_tag(@next_photo.picture(:original), :class => "next fullBg")
=render "main/main_page_menu"
.curtain
.backup_curtain
